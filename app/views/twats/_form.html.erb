<%= form_with(model: twat, local: true) do |form| %>
  <% if twat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twat.errors.count, "error") %> prohibited this twat from being saved:</h2>

      <ul>
      <% twat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
